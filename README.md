# docker-images-for-globaleaks
Demo repo for building versioned docker images

### Building the image
Simply run the following command to build and start globaleaks:
```sh
docker compose up --build
```

Access globaleaks at http://localhost
